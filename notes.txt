
Let's do this:
* Click on event to select it
* Zoom to a nice event view, framing event in a map hole
* Zoom on the timeline at the same time


Issues:
* Map scroll wraps but events don't


TODO at some point:
* Get docker working properly with UID/GID
* Use a land-sea mask to mark events which are land-only/sea-only
* Split event into individual days (easy? very easy?) when zoomed for time axis
* Colour events by chooser
* Settings? Show help, unlock things, etc?
* Big filter. Filter should be an important part - we should have too many events by default, and filter them sensibly to start with
    - intensity
    - size
    - duration
    - area affected


