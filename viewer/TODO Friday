TODO Friday


Time scrubber
DONE

Single event histogram - lock the y-scale to the max event value - add caching of all histogram data while you're at it?

Can I reverse the histogram calculation to get every event within a histogram bar and then highlight all of those events everywhere? Would that be cool as fuck?

Olde-world version of data - need to generate and make selectable from the hamburger menu. Should just be as simple as triggering main method in eventDB

Optimisation - pre-render all the grid tiles when the first one gets triggered for an event. Cache: key+event -> event-times -> ImageBitmap, then drawImage(ImageBitmap) is GPU-bound

A zoom-to-event button which cycles the view between all current events
DONE - Use local event ranker for same effect

When zoomed in on event on timeline, add next years event bars if required (or just anyway?)

Download CSV from scatter. And histogram?  Make PNG exportable - going to have to render to canvas, but it's doable

July 11th 2023 - good example for research (over USA)


BUG ALERT!!!!
If event has days with 100% ocean, they get removed and times.length doesn't match slices



More hamburger menu items:
    Variable definitions - various for intensity
    Put some filters back! DONE
    Animation speed controls
    Saved settings


Scatter plot export

Scatter plot explore nearby points


USER NOTES:

MultiEvent is overwhelming

Heatmap isn't interactive enough - add a moving time window of ~10 years. Then render heatmap data by year for ultrafast switching?

TimeReel - vertical dragging

Contextual help - use shepherd JS or maybe just Floating UI (which shepherd is built upon)

Hide MultiEventPanel button

Keep region selection onscreen to show what is selected.
AND/OR Make it more obvious how many events are currently selected, and where they are selected
Perhaps have a little preview map in the corner? Maybe not that's a bit of a space hog

Event ranking - only when event selected. Put it somewhere common, along with event stats (bottom still? top left?)
Histograms, scatters, etc. - always on (hideable) with heatmap view, but show fewer by default. MEP can then contain region selection tools, to link those two things more clearly - it will be obvious what kind of region is selected.

Double up the filters to allow things like 4 < duration < 10

Make panels have a rainbow shine minus the theme colour, then tone the saturation or opacity right down, they shouldn't be perceptible. DONE




PRECIP DEF - Climatology, 95th%ile of wet days (- precip < 1mm), 75%, 25%
IQR is 75%-25%
So (precip - 95%) / IQR = PRECIP
NO 3 DAY LIMIT

30 X 1x1 deg minimum area for heat

Do the AREA properly and use units of degrees squared (at the equator) 1°x1° ("degree cell equivalent")



HEAT INDEX: DEI anomaly over 95th%ile per day, added up per pixel

mean intensity * mean duration * mean area

Intensity = degrees over threshold / SD at that pixel


Climatology has a 30 day window and is on a daily basis.


MINMAX not changing

Named regions, and use as default comparison

ROADMAP + EXISTING FUNCTIONALITY

+ ROADMAP for P1